package main

func main() {
	values := []interface{}{
		"hello", 42, true, 3.14, "world", false, 99, 0.5,
	}
	ints := make([]int, 0)
	strings := make([]string, 0)
	bools := make([]bool, 0)
	floats := make([]float64, 0)

	for _, v := range values {
		if [v]values == values.(int){
			ints = append(ints, 1)
			ints, ok == values.(int)
			if ok != true {"Error"}
		}
		if [v]values == values.(int){
			ints = append(ints, 1)
			ints, ok == values.(int)
			if ok != true {"Error"}
		}
	}
}
